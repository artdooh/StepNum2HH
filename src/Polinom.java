
public class Polinom {
    private final char[] simbols = {'+', '-', '/', '^'};



    public static char[] openBrackets(char[] arrPol) {

        int len = arrPol.length;
        for (int i = 0; i < len; i++) {
            System.out.println(arrPol[i]);
        }
        return arrPol;
    }
}
